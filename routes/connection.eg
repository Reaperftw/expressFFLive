var mysql = require('mysql');
require('log-timestamp');

var pool = mysql.createPool({
  host        : '127.0.0.1',
  port        : 8080,
  user        : 'matt',
  password    : 'password',
  database    : 'FFLiveDev'
});

module.exports = exports = pool;
